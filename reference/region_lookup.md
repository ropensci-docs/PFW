# Region Lookup Table

The region lookup table, which maps SUBNATIONAL1_CODE values to region
"common" names.

## Usage

``` r
region_lookup
```

## Format

A data frame with 2 columns:

- Code:

  Region code (e.g., "US-WA")

- Area:

  Full area name (e.g., "Washington")
